version: "3.9"

services:
  postgres:
    image: postgres:16
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: cribcove
      POSTGRES_PASSWORD: cribcove
      POSTGRES_DB: cribcove
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:
